# cp .env.example .env. Never commit the filled-in file.
POSTGRES_PASSWORD=replace-me
# the admin credentials created at first-run setup; kestra_bench.py reads
# KESTRA_USER / KESTRA_PASSWORD from its own environment, not from this file.
KESTRA_PASSWORD=replace-me
